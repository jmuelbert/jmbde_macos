coverage:
  status:
    patch:
      framework:  # declare a new status context "framework"
        threshold: null
        paths: "JMBde/"  # only include coverage in "TaylorFramework/" folder
    project:
      framework: 
        target: 90%  # we always want 90% coverage here
        paths: "JMBde/"