language: swift
osx_image: xcode10
xcode_project: jmbde.workspace
xcode_scheme: jmbde
xcode_destination: platform=macOS

branches:
  execpt:
    - gh-pages

before_install:
- gem install bundler
- bundle install
- bundle exec pod repo update --silent
- brew update
- brew outdated xctool || brew upgrade xctool
- brew outdated carthage || brew upgrade carthage
- git remote set-url origin "https://${GITHUB_TOKEN}@github.com/jmuelbert/jmbde-macos.git"

install:
  - gem install xcpretty

env:
  global:
    - LC_CTYPE=en_US.UTF-8
    - LANG=en_US.UTF-8
    - FRAMEWORK_NAME="jmbde"
    - UPDATE_DOCS="true"
    - EXPANDED_CODE_SIGN_IDENTITY="-"
    - EXPANDED_CODE_SIGN_IDENTITY_NAME="-"

matrix:
  include:
    - osx_image: xcode10.1
      env: SCHEME="jmbde"  SDK="macosx10.14"          DESTINATION="arch=x86_64"                    SWIFT_VERSION="4.2.1"

notifications:
  email: false



script:
  - set -o pipefail
  - xcodebuild -version
  - xcodebuild -showsdks
  - xcodebuild
    -workspace "$FRAMEWORK_NAME.xcworkspace"
    -scheme "$SCHEME"
    -sdk "$SDK"
    -destination "$DESTINATION"
    -configuration Debug
    ONLY_ACTIVE_ARCH=YES
    GCC_INSTRUMENT_PROGRAM_FLOW_ARCS=YES
    GCC_GENERATE_TEST_COVERAGE_FILES=YES
    SWIFT_VERSION=$SWIFT_VERSION
    test

after_success:
- swift --version
- swiftlint lint
 - bash <(curl -s https://codecov.io/bash) -J JMBde
  - test $TRAVIS_PULL_REQUEST == "false" &&
    test $TRAVIS_BRANCH == "master" &&
    test $UPDATE_DOCS == "true" &&
    .scripts/update-gh-pagespretty